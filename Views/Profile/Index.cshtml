@using  Messege.ViewModels
@model  ProfileView

    <div class="row">
        <div class="col-sm-3"></div>
        <div class="col-sm-6">
            
            <div class="container text-center mt-3">
                <img src="/@Model.Profile_Picture" height="100" width="100" class="rounded-circle" />
                <div> @Model.Email</div>
                <b>@Model.First_Name @Model.Last_Name</b>
            </div>
        </div>
        <div class="col-sm-3"></div>
    </div>

